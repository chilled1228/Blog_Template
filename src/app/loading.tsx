import { Layout } from '@/components/layout';
import HomePageLoading from '@/components/ui/HomePageLoading';

export default function Loading() {
  return (
    <Layout>
      <HomePageLoading />
    </Layout>
  );
}